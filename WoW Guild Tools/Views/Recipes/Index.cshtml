@model IEnumerable<WoW_Guild_Tools.Models.Recipe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.ProfessionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.WowRecipeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WowCraftedItemId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.ProfessionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>*@
            <td>
                <a href="@($"https://tbc.wowhead.com/{item.Type.ToString().ToLower()}={item.WowRecipeId}")">@Html.DisplayFor(modelItem => item.WowRecipeId)</a>
            </td>
            <td>
                <a href="@($"https://tbc.wowhead.com/item={item.WowCraftedItemId}")">@Html.DisplayFor(modelItem => item.WowCraftedItemId)</a>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
